import React, { Components } from 'react'

class Supported extends Components {
    render() {
        return (
            <div>
                <h1>Supported</h1>
            </div>
        )
    }
}

export default Supported;